<% @title = '記事の作成' %>
<h1><%= @title %></h1>

<div>
    <%= form_with model: [@posts.user, @post] do |f| %>
    <%= render 'form', f: f %>
    <div class='button'>
        <%= f.submit '記事を更新' %>
    </div>
    <% end %>
</div>